apiVersion: 1

datasources:
  - name: SQLite
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    jsonData:
      path: /data/nmap_results.db
  - name: SQLite2
    type: frser-sqlite-datasource-1
    access: proxy
    isDefault: true
    jsonData:
      path: /data/nuclei_results.db

